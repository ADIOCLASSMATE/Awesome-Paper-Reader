[project]
name = "awesome-paper-reader"
version = "0.1.0"
description = "Academic paper reading and knowledge management with GROBID PDF parsing"
requires-python = ">=3.14"
dependencies = [
    "docling>=2.88.0",
]

[[tool.uv.index]]
url = "https://mirrors.aliyun.com/pypi/simple/"
default = true
